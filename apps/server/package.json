{
  "name": "@etf-titan/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others --names \"API,WEB\" --prefix \"[{name}]\" \"bun run dev:api\" \"bun run dev:web\"",
    "dev:api": "if [ -x .venv/bin/python ]; then .venv/bin/python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000; else python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000; fi",
    "dev:web": "bun --cwd ../web dev"
  }
}
